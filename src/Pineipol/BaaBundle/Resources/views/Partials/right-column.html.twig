<div id="links-title-box">Enlaces relevantes</div>
<div id="links-container">
    {% for link in links %}
        <a class="link" href="{{ link.targetUrl }}" title="{{ link.description }}">{{ link.title }}</a>
    {% endfor %}
</div>